# /third-party-models

Run the installed gateway command to list all third-party models and print Claude Code setup instructions.

```bash
claude-code-model-gateway third-party-models
```

Show the output to the user exactly as printed. If the command exits with an error or says no config was found, suggest running `claude-code-model-gateway config init` first.
